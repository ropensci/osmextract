# An sf object of geographical zones taken from download.openstreetmap.fr

This object represent a minimal provider's database and it should be
used only for examples and tests.

## Usage

``` r
test_zones
```

## Format

An object of class `sf` (inherits from `data.frame`) with 2 rows and 7
columns.
